import { DiaFechaPipe } from './dia-fecha.pipe';

describe('DiaFechaPipe', () => {
  it('create an instance', () => {
    //const pipe = new DiaFechaPipe();
   // expect(pipe).toBeTruthy();
  });
});
